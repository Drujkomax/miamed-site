window.addEventListener('DOMContentLoaded', () => {
    const msgEl = document.getElementById('message');
    if (msgEl) {
        msgEl.textContent = 'Page loaded successfully!';
    }
    console.log('Miamed site loaded');
});
